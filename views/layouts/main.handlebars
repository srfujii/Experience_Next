<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Experience Next</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@546&display=swap" rel="stylesheet">

</head>

<!-- Our STATIC HTML displayed on every page -->
<body class="site">
    
    <header class="display-flex justify-space-between align-center p-4 bg-primary jumbotron">
      <h1 class="text-white">Experience Next</h1>
      <nav>
        <a class="mr-5" href="/">Home</a>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a class="mr-5" href="/dashboard">Dashboard</a> |
        <a class="mr-5" id="logout" href="#">Logout</a>
        <div class="dropdown">
        <button onclick="myFunction()" class="expBtn">Choose an Experience
        </button>
        <div class="dropdown-content">
        <a href="/api/experience/Tattoo">Tattoo</a>
        <a href="/api/experience/Personal Chef">Personal Chef</a>
        <a href="/api/experience/Massage">Massage Therapy</a>
        <a href="/api/experience/House Cleaning">Cleaning</a>
        <a href="/api/experience/Exorcism">Exorcisms</a>
        </div>
        </div> 
        {{else}}
        <a class="mr-5" href="/login">Login</a>
        {{/if}}
      </nav>
    </header>

    <main class="outer-container site-content">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <footer class="jumbotron footer"></footer>

  <!-- Render logout script for logged in users only -->
  {{#if logged_in}}
    <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
